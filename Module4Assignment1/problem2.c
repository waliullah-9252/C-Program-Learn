#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */
    long long int a,b,multi;
    scanf("%lld %lld",&a,&b);
    multi = a*b;
    printf("%lld",multi);
    return 0;
}
