#include<stdio.h>
#include<math.h>
#include<stdlib.h>
int main()
{
    // double x;
    // scanf("%lf",&x);
    // int ans=ceil(x);
    // int ans=floor(x);
    // int ans=round(x);

    // int x;
    // scanf("%d",&x);
    // double ans=sqrt(x);
    // printf("%.2lf",ans);

    // double a,b;
    // scanf("%lf %lf",&a,&b);
    // double ans=pow(a,b);
    // printf("%.2lf",ans);

    int x;
    scanf("%d",&x);
    int ans=abs(x);
    printf("%d",ans);
    return 0;
}